{
  // let size = parseInt(prompt("enter array size"));
  // let arr = [];
  // let sum = 0;
  // for (let i = 1; i <= size; i++) {
  //   arr.push(parseInt(prompt("enter array element")));
  // }
  // console.log(arr);
  // let total = arr.map((item) => (sum += item));
  // console.log(`sum : ${total[total.length - 1]}`);
  // let min = Math.min(...arr);
  // let max = Math.max(...arr);
  // console.log(`max number is ${max}`);
  // console.log(`min number is ${min}`);
  // let keyword = parseInt(prompt(`enter what do you want:`))
  // let serch = arr.includes(keyword)
  // if (serch === true)
  // {
  //   console.log(`found ${keyword} in array`);
  //   }
  //   else
  //   {
  //     console.log(`not found ${keyword} in array`);
  //     }
}

{
  // let array = []
  // let size = parseInt(prompt("enter array size"))
  // for (let i = 0; i < size; i++) {
  //   array[i] = Math.floor(Math.random()*100)
  // }
  // document.writeln("Array is:",array)
}

{
  
  // let array = [5, 234, 56, 78, 4, 3, 56, 781, 45];
  // document.writeln("Array is:",array,"<br>")

  // let array = [];
  // let size = parseInt(prompt("Enter array size"));
  
  // let isThreeDigit = confirm("Do you want only three-digit numbers?");
  // if (isThreeDigit === true){
  //   for (let i = 0; i < size; i++) {
  //     array[i] = Math.floor(Math.random() * 900) + 100; 
  //   }
  // } 
  // if (isThreeDigit === false){
  //   for (let i = 0; i < size; i++) {
  //     array[i] = Math.floor(Math.random() * 100); 
  //   }
  // }
  
  // document.writeln("Original Array: " + array.join(", ") + "<br>");
  
  // let n = array.length;
  // let temp;
  // for (let i = 0; i < n - 1; i++) {
  //   for (let j = 0; j < n - i - 1; j++) {
  //     if (array[j] > array[j + 1]) {
  //       temp = array[j];
  //       array[j] = array[j + 1];
  //       array[j + 1] = temp;
  //     }
  //   }
  // }
  
  // document.writeln("Sorted Array: " + array.join(", ") + "<br>");
  // document.writeln("Max number is: " + array[n - 1] + "<br>");
  // document.writeln("Min number is: " + array[0] + "<br>");
  

}
